#include "main.h"
/**
 * add - add two integer and return the result
 * @a: first parameter
 * @b: second parameter
 * Description: add to numbers
 * Return: Always (0)
 */
int add(int a, int b)
{
	return (a + b);
}
